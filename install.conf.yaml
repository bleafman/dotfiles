- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zprofile: zprofile
    ~/.gitconfig: .gitconfig
    ~/.gitignore_global:
    ~/.shell:
    ~/.zshrc:
    ~/.claude/commands: claude/commands
    ~/.claude/hooks:
      path: claude/hooks
      force: true
